import ExpenseItem from "./ExpenseItem";

const Expenses = (props) => {
    console.log(props)
     

    return (
        <div >
           <ExpenseItem/>
        </div>
    )
}

export default Expenses